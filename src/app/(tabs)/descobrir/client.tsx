'use client'

import React from 'react'

export default function DescobrirClient(props: { filters?: unknown; [key: string]: any }) {
  return <div style={{ padding: '8px' }}>Descobrir — client ready</div>
}
