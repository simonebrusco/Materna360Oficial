// next.config.mjs ou next.config.js (com "type": "module" no package.json)
const nextConfig = {
  reactStrictMode: true,
  // coloque aqui outras configs que você usava (images, headers, redirects etc.)
};

export default nextConfig;
